name: lazy_wrap
description: A Flutter widget that mimics Wrap layout behavior with lazy rendering using ListView.builder.
version: 0.0.9+1
homepage: https://github.com/henselldev/lazy_wrap
license: MIT

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
